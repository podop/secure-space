# Reference — secure-space (umbrella)

Umbrella-level lookup tables: port allocations across projects, shared configuration keys, glossary of security terms used consistently across services.

## Suggested initial files

- `port-map.md` — which project listens where in local Docker Compose
- `glossary.md` — CA, CSR, CRL, OCSP, rolling, expiration, …

*(files are added as the workspace grows)*
